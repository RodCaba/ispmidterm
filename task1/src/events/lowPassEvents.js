function setLowPassFrequency(freq) {
  lowPassFilter.freq(freq);
}

function setLowPassResonance(res) {
  lowPassFilter.res(res);
}

function setLowPassDryWet(dryWet) {
  lowPassFilter.drywet(dryWet);
}

function setLowPassOutputLevel(level) {
  lowPassFilter.amp(level);
}